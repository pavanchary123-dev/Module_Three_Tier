**If you unable to push the code to your repo follow this process**
rm -rf .git
git init
git add .
git commit -m "Initial clean commit"
git remote add origin https://github.com/pavanchary123-dev/Module_Three_Tier.git
git push -u origin main
