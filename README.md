**If you unable to push the code to your repo follow this process**

1.rm -rf .git
2.git init
3.git add .
4.git commit -m "Initial clean commit"
5.git remote add origin https://github.com/pavanchary123-dev/Module_Three_Tier.git
6.git push -u origin main
